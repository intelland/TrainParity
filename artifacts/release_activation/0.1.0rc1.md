# TrainParity 0.1.0rc1 release-activation audit

## Outcome

**PASS — TWO BOUNDED RELEASE BLOCKERS CORRECTED**

Publication remains held. This audit is not a new feature Gate and did not
upload to PyPI, create a tag or GitHub Release, merge a branch, or change
repository visibility.

## Documentation blocker

- README now identifies 0.1.0rc1 as an alpha prerelease and gives
  `pip install trainparity==0.1.0rc1`.
- Release-facing held/unpublished statements were removed from README,
  CHANGELOG, and the 0.1.0rc1 release notes.
- README repository links are absolute GitHub URLs suitable for PyPI rendering.
- The rebuilt sdist README and wheel METADATA contain the exact install command
  and absolute validation link, with no stale unpublished-candidate statement.

## Same-artifact blocker

The manual release workflow builds once, selects exactly one `dist/*.whl`,
installs that wheel into a fresh environment, changes to `$RUNNER_TEMP`, checks
the installed version against `pyproject.toml`, and runs the resume,
accumulation, and sample-coverage quickstarts before the pinned publishing
action receives the unchanged `dist/` directory. Release concurrency is
serialized and publication still requires main, the protected `pypi`
environment guard, human approval, and job-scoped OIDC.

## Verification

- Source commit: `0203e9d3dc81c9a5fd199806ab020ca0065e0b83`
- M3 job: `59014365`, `COMPLETED`, exit `0:0`, elapsed `00:16:31`
- Ruff: PASS
- strict Mypy: PASS
- tests: 147 passed; coverage 90.31%
- README integration and three quickstarts: PASS
- wheel install outside repository: PASS
- repository/archive audit: PASS with zero blockers
- wheel and sdist Twine checks: PASS
- accepted Gate 0–7 and Gate 7I verifiers: PASS
- accepted Gate 7I bundle SHA-256 remains
  `22a2a788ae1a90ab9ac1d33f797ef48ecdafe7ad18b7e6e1df2714f21c2b8879`
- user document SHA-256 remains
  `6b532b94660949abec3c50bbe826d2154a797eeec744d3e5c91058dec9a96300`

Rebuilt artifact hashes:

- wheel: `c3e2e30f50848b8a81403a9c01cf5990b0a5aabad347239f806f73b64b3a5529`
- sdist: `cefa5ccacea52ee8f01206d693878c63b6627c24b8eb057c205a8eacff3cfbf0`

## Remaining publication boundary

Before publication, a human still must approve the final commit, configure
the protected GitHub `pypi` environment reviewers and PyPI Trusted Publisher,
recheck name availability, and explicitly authorize the workflow run.
