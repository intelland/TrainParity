# Name availability check

Checked on 2026-08-11 before preparing 0.1.0rc1 metadata:

- `https://pypi.org/pypi/trainparity/json` returned HTTP 404, so no PyPI project
  was visible under the normalized name at check time.
- GitHub repository name search returned only the existing private
  `intelland/TrainParity` repository.
- The existing repository remained private with default branch `main`.

These observations are time-limited and do not reserve a package or repository
name. No rename, PyPI upload, tag, GitHub release, or visibility change was
performed.
