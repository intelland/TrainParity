# Development records

This directory preserves the human-defined plan, acceptance criteria, gate
status, decisions, and Codex goals used while developing TrainParity. These are
development provenance, not the user-facing product documentation.

Accepted machine evidence remains under `artifacts/gate_reports/` and recorded
experiment outputs remain under `experiments/`. Neither location is included
in the runtime wheel or source distribution.
